# Used by `vite build` (e.g. Vercel). Points the browser to your Render API for REST + Socket.io.
# Override in Vercel → Settings → Environment Variables if needed.
VITE_API_BASE=https://interviewx-gpaf.onrender.com
